var names = ["David", "Cynthia", "Raymond", "Clayton", "Jennifer"];

var position = names.indexOf('Raymond');
if (position >= 0) {
    print("Found " + 'Raymond' + " at position " + position);
}
else {
    print('Raymond ' + " not found in array.");
}